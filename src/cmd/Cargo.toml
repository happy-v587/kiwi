[package]
name = "cmd"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = { workspace = true }
bitflags = "2.11.1"
num-traits = "0.2"
bytes = { workspace = true }
storage = { path = "../storage" }
client = { path = "../client" }
resp = { path = "../resp" }
conf = { path = "../conf" }
error-catalog = { path = "../common/error-catalog" }
subtle = "2.5"
thiserror.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["full"] }
async-trait = "0.1"
